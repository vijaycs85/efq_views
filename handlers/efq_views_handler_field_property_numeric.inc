<?php
// $Id$

/**
 * Numeric field handler
 */
class efq_views_handler_field_property_numeric extends views_handler_field_numeric {
  /**
   * Override the parent's query method, since it doesn't need to do anything.
   */
  function query() {}
}